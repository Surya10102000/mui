import { createTheme } from '@mui/material/styles';
const theme = createTheme({
  palette: {
    primary: {
      main: "FF71CD",
      
      
    },
    secondary: {
      main: '#5755FE',
      light: '#F5EBFF',
      contrastText: '#47008F',
    },
    other: {
        main : "8B93FF"
    }
  },
});

export default theme